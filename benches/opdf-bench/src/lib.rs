//! Host crate for open-pdf-editor's `criterion` benchmarks.
//!
//! This crate has no public API of its own; it exists so `benches/*.rs` has
//! a package to belong to. See `benches/tile_alloc.rs` and
//! `benches/vec_document_mutations.rs` for what is actually measured.
